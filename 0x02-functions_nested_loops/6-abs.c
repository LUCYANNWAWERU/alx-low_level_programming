#include "main.h"


/**
 *_abs -  _abs prints out the absolute value
 *@n: interger to make absolute
 *  Return: return the absolute value
 */
int  _abs(int n)
{
	if (n < 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
